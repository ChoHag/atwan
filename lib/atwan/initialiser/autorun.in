#!sh
# _sourced_ by the initialiser script on the new server.
#
# The steps to execute on the new server are appended to this file.

if [ -z "$at_top" ]; then
  echo at_top is not set. Don\'t know where to look. >&2
  exit 1
fi

### HEADER END
